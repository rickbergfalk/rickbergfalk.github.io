<% include layout-header %>
	
	<section id="main_content">
		
		<div class="section">
			<h2>Projects</h2>
			<h3>
				<a href="http://www.readysetsql.com/">Ready Set SQL</a>
			</h3>
			<small>Interactive SQL Lessons (2011 - 2014)</small>
			<br><br>
			<div>
				<p>
					Ready Set SQL is a series of interactive SQL lessons geared towards learning SQL useful for reporting purposes. 
					The target audience is semi-tech-savvy business people who may have access to some sort of database (either directly or via some tool such as Microsoft Access).
				</p>
				<p>
					It's mostly finished. Just needs more content and some final tweaking.
				</p>
			</div>
		</div>
		
		<hr>
		<div class="section">
			<h2>
				 Notes, Thoughts, Ramblings, etc.
			</h2>
			<% 
				// a reverse loop, so new stuff is at the top
				for (var i = 0; i < posts.length; i++) {
					var post = posts[posts.length - 1 - i];
					%>
						<div class="post-card <%= post.meta.category.replace(/ /gi, '-') %>">
							<a class="title" href="/posts/<%= post.filename %>.html"><%= post.meta.title %></a>
							<!--
							<span class="category hide"><%= post.meta.category %></span>
							<span class="date hide"><%= moment(post.meta.date).format("MMMM YYYY") %></span>
							-->
						</div>
					<%
				}
			%>
		</div>
		
	</section>

<% include layout-footer %>